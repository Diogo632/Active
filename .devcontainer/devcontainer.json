{
  "name": "Base de Conhecimento Active",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "postCreateCommand": "npm ci && (test -f .env || cp .env.example .env)",
  "forwardPorts": [
    3000
  ],
  "portsAttributes": {
    "3000": {
      "label": "Base de Conhecimento",
      "onAutoForward": "openBrowser"
    }
  },
  "secrets": {
    "N8N_WEBHOOK_URL": {
      "description": "URL de produção do webhook do n8n que chama o agente do GPTMaker."
    },
    "N8N_WEBHOOK_TOKEN": {
      "description": "Opcional: token Bearer exigido pelo webhook do n8n."
    },
    "INTEGRATION_TOKEN": {
      "description": "Opcional: token para o n8n consultar a base pela API de integração."
    }
  }
}
